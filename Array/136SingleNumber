class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        xor=0
        for i in range(0,len(nums)):
            xor=xor^nums[i]
        return xor
        
#1^1^2^3^3^4^4
#0^2^0^0
#2
